{
	"name": "Existing Dockerfile",
	"context": "..",
	"dockerFile": "Dockerfile",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-python.vscode-pylance",
				"ms-python.black-formatter",
				"ms-python.pylint",
				"ms-python.isort",
				"ms-vscode.cpptools",
				"ms-vscode.cpptools-extension-pack",
				"streetsidesoftware.code-spell-checker",
				"ms-azuretools.vscode-docker",
				"GitHub.vscode-github-actions",
				"xaver.clang-format"
			]
		}
	},
	"containerEnv": {
		"SHELL": "/bin/bash"
	},
	"initializeCommand": "mkdir -p \"${localWorkspaceFolder}/../RTLSDR-Airband_test_cases\"",
	"postCreateCommand": "pre-commit install",
	"postStartCommand": "cd ${containerWorkspaceFolder}/system_tests && uv sync",
	"mounts": [
		"source=${localWorkspaceFolder}/../RTLSDR-Airband_test_cases,target=${containerWorkspaceFolder}/system_tests/test_cases,type=bind,readonly"
	],
	"runArgs": [
		"--cap-add=SYS_PTRACE",
		"--security-opt",
		"seccomp=unconfined"
	]
}
